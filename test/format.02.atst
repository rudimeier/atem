## -*- shell-script -*-

TOOL=atem
INFILE="msdir_equis_a"
OUTFILE="testload.csv"
CMDLINE="--fdat 1 -F, --format='+long_name,-time,-openint' '${INFILE}'
	|head -n 1"

## STDIN

## STDOUT
stdout=$(mktemp "/tmp/tmp.XXXXXXXXXX")
cat > "${stdout}" <<EOF
symbol,long_name,date,open,high,low,close,volume
EOF

## outfile sum
