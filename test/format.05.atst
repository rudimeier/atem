## -*- shell-script -*-

TOOL=atem
INFILE="msdir_equis_a"
# TODO fix test suite to respect the exit code _and_ stderr
CMDLINE="--fdat 1 -F, --format='+long_name,bad_column' '${INFILE}' 2>&1
	| head -n 1 1>&2"

## STDIN

## STDOUT
touch "${TS_EXP_STDOUT}"

## STDERR
cat > "${TS_EXP_STDERR}" <<EOF
error: invalid format token: bad_column
EOF

## outfile sum
